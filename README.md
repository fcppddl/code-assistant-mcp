# 飞书文档 MCP

PSS 项目代码助手

## 在 Cursor 中使用

### MacOS / Linux

```json
{
  "mcpServers": {
    "code-assistant": {
      "command": "npx",
      "args": ["-y", "code-assistant-mcp", "--mify-api-key=YOUR-KEY", "--stdio"]
    }
  }
}
```
